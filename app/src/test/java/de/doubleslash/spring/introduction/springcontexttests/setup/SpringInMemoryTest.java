package de.doubleslash.spring.introduction.springcontexttests.setup;

/*@Getter
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.DEFINED_PORT,
        properties = {"spring.datasource.url=jdbc:h2:mem:springintroduction://localhost:9092"})
@ActiveProfiles("test")
//@DataJpaTest(properties = {"spring.datasource.url=jdbc:h2:mem:springintroduction://localhost:9090"})
@ComponentScan("de.doubleslash.spring.introduction")
@EnableAutoConfiguration
//@ContextConfiguration(classes = {FileHandlerConfiguration.class})
public abstract class SpringInMemoryTest {
    @BeforeEach
    public void beforeEach() {
        System.out.println("Test started: " + this.getClass().getSimpleName());
    }

    @AfterEach
    public void afterEach() {
        System.out.println("Test finished: " + this.getClass().getSimpleName());
        //if (!repository.findAll().isEmpty()) repository.deleteAllInBatch();
    }
}*/
